# mcp_config.py

CONFIG = {
    "mcpServers": {
        "local": {
            "url": "http://localhost:8000/mcp/" #if you want to access using the docker add the name of the server docker container  
        }
    }
}
